#!/usr/bin/env python3
"""Module containing the concat function for two strings."""


def concat(str1: str, str2: str) -> str:
    """Returns the concatenation of two strings."""
    return str1 + str2
